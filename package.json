{
    "name": "discovery-adcet",
    "version": "1.0.0",
    "description": "registration portal for discovery adcet",
    "scripts": {
        "build": "cd frontend && npm install && npm run build"
    }
}